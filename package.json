{
  "name": "bebe-taxi",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "react-native": "0.86.0"
  },
  "scripts": {
    "backend": "npm run dev --workspace=apps/backend",
    "mobile": "npm run start --workspace=apps/mobile",
    "typecheck": "npm run typecheck --workspace=apps/backend && npm run typecheck --workspace=apps/mobile",
    "doctor": "npm run doctor --workspace=apps/mobile",
    "clean": "npx rimraf node_modules apps/backend/node_modules apps/mobile/node_modules packages/shared/node_modules apps/backend/.next apps/backend/dist"
  }
}